node_modules
build
public/build
shopify-app-remix
*/*.yml
.shopify

# Extensions have their own toolchain (Shopify CLI build + vitest) and generated code.
extensions

# One-off ops scripts (plain Node .mjs, run with `node --env-file`).
scripts
